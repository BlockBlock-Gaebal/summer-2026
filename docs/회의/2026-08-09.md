# 회의록 — 2026-08-09

- **참석**: 하진모, 정승준, 김유안
- **성격**: 인터페이스 프리즈 회의
- **원칙**: 파트 내부 결정은 각자 재량. 회의는 **혼자 정하면 남이 되돌려야 하는 것**만 다룬다.

---

## 1. 진행 상황

| 파트 | 담당 | 상태 |
|---|---|---|
| A. 컨트랙트 | 진모 | ver1~4 완료, `sui move test` 24/24 통과, Testnet 배포 완료, 설계 결정 전량 확정 |
| B. 대시보드 | 승준 | 미착수 (`frontend/` 코드 0줄) |
| C. 오라클·데모 | 유안 | 온보딩 완료 |

> 참고: `frontend/`, `scripts/`는 README만 main에 있고(PR #10, 커밋 `dd9a9d6`) 실제 코드는 아직 0줄이다.
>
> *(2026-08-10 정정: 원문은 "두 디렉토리가 main에 반영되어 있지 않음"이라고 적었으나 사실이 아니었다.
> 회의 시점에 이미 `frontend/README.md`·`scripts/README.md`가 main에 있었다.)*

---

## 2. 결정 사항

상세는 `docs/DECISIONS.md` 참조. 요약:

1. **인터페이스 프리즈 선언** — 함수 시그니처 / 이벤트 필드 / struct 필드가 대상. 배포 ID·파라미터 값·내부 구현·UI는 비대상
2. **대시보드 1순위 확정** — CLI 실연 경로는 제거하지 않고 유지 (백업)
3. **대시보드는 읽기 전용** — 지갑 서명 트랜잭션 미구현
4. **데모는 사전 세팅 상태에서 시작** — 무대에서 방 생성/참여 안 함
5. **표시용 계산은 프론트, 금액 계산만 온체인**
6. **배포 ID 정본 = `docs/DEPLOYMENT.md`**
7. **참가자 Table 조회 스크립트는 진모가 공용 제공**

---

## 3. 미결 / 보류

- [x] **빈 방 `submit_results` 가드 추가 여부** (판단: 진모) → **미도입 확정** (2026-08-10, D-22)
  - 재배포 비용이 예상보다 크다: `Published.toml`이 커밋된 뒤로 `sui client publish`가
    `Your package is already published`로 거부하므로 `[published.testnet]` 항목을 지우는 선행 작업이 필요하다.
    게다가 로컬 CLI 프로토콜 버전(129)이 testnet(132)보다 낮아 재배포 시 의존성 검증 오류 위험도 있다
  - 데모는 사전 세팅으로 진행하므로(D-18) 발표 중 발생 가능성은 없음
  - 대신 **운용 규칙("빈 방에 submit 금지")으로 회피**. `read_state.ts`가 빈 방을 감지하면 경고를 출력한다
- **day1 탈락자 claim abort** — 버그 아님. `k = d-1` 커브상 완주일수 0 → 환급 0이 정의된 동작. 대시보드에서 버튼 비활성 + "전액 몰수" 표시로 처리 (승준)
- **코인 오브젝트 1개일 때 split 실패** → **해법 교체됨** (2026-08-10, D-24).
  회의에서 정한 "faucet 복수 수령으로 코인 분할"은 **testnet faucet CLI가 폐지되어 무효**다.
  확정 해법은 PTB에서 가스 코인을 직접 쪼개는 것 — 코인 오브젝트가 1개여도 동작한다 (실측 확인).
  상세는 `scripts/README.md`

---

## 4. 다음 회의까지 각자 할 일

### 진모 (컨트랙트 / 설계)

컨트랙트 구현은 완료 상태. 이번 구간의 본업은 **다른 파트 언블록 + 발표 자산**.

1. **`scripts/read_state.ts` (최우선)**
   - Challenge ID 입력 → 참가자 배열 출력
   - `getDynamicFields` → `getDynamicFieldObject` 2단계 조회
   - 프론트·오라클 공용
   - **완료 정의**: 실행하면 참가자들의 주소 / stake / failed_day / claimable이 콘솔에 출력된다
2. **문서 PR** — `DEPLOYMENT.md` 신규 + `DECISIONS.md` 갱신 + 본 회의록. 리뷰어 승준·유안 둘 다
3. **가드 패치 여부 최종 판단** — 넣는다면 승준 착수 전에 완료
4. (여유) 발표 5분 대본 초안

### 유안 (오라클 / 데모) — 승준보다 먼저 나와야 함

승준이 화면을 검증하려면 **상태가 실제로 변해야** 한다. 전원 생존 상태로는 탈락 표시·claimable 확인이 불가하므로 이 파트가 선행.

1. **`scripts/seed.ts`** — 방 생성 + 3인 join 자동화
   - 리허설마다 새 방을 파야 하므로 수동 진행은 비현실적
   - 실행할 때마다 **동일한 상태**가 재현되어야 함
   - 코인 오브젝트 복수 확보 후 진행 (split 실패 회피)
   - **완료 정의**: 명령어 한 줄로 새 방 + 3인 참여 상태가 만들어진다
2. **`scripts/submit.ts`** — day별 탈락자 제출
   - 시나리오: day1 B탈락 / day3 C탈락 / A완주
   - **완료 정의**: 실행 후 `read_state.ts`에서 failed_day와 claimable 변화가 확인된다
3. **데모 대본 v1** — 5분 안에 무엇을 어떤 순서로, 누가 말하고 누가 조작하는지 (초안 수준)

### 승준 (대시보드) — 크리티컬 패스

읽기 전용. 트랜잭션 전송 미구현.

| 순위 | 작업 | 완료 정의 |
|---|---|---|
| 1 | vite + `@mysten/sui` 세팅, Testnet gRPC 연결 | Challenge 오브젝트가 콘솔에 찍힘 |
| 2 | ID를 config 한 곳으로 분리 | 코드 내 ID 하드코딩 0건 |
| 3 | **참가자 목록 표 렌더** | 주소/예치금/상태가 표에 표시 ← **최소 성립선** |
| 4 | 방 정보 표시 | current_day / vault / status |
| 5 | claimable + 탈락일 | day1 탈락자 0원 표시 |
| 6 | 새로고침 or 폴링 | submit 후 값이 갱신됨 |
| 7 | 디자인 | — |

- 참가자 조회는 진모의 `read_state.ts`를 받아서 사용. 혼자 붙잡지 말 것
- 환급률 등 파생값은 프론트에서 계산
- 디자인은 마지막. 데이터가 먼저
- PR은 단계별로 쪼갤 것 (1~3을 한 PR로 묶지 않기)

---

## 5. 공통 운영 규칙 (이번 구간)

- **파트 내부에서 막힘** → 30분 룰, 단톡
- **남의 파트 결정이 필요해서 막힘** → 즉시 태그, 대기 금지. 회의까지 들고 오면 며칠이 날아간다
- 다음 회의에서는 각자 **위 완료 정의 문장이 참인지 화면공유로 확인**. 구두 보고로 갈음하지 않는다
- 통합이 예정대로 되지 않을 경우, **일정을 미루지 않고 스코프를 줄인다** (사전 합의)

---

## 6. 다음 회의

- 일시: (미정 — 별도 합의)
- 주요 안건: 각자 완료 정의 검증, 통합 시점 확정, 데모 대본 확정
