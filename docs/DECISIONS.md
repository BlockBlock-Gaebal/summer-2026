# DECISIONS.md — 확정 결정 + 현재 상태

> **확정 사항의 유일한 정본이다.** 다른 문서와 충돌하면 이 파일이 이긴다.
> 결정은 지우지 않는다. 뒤집힐 경우 새 항목을 추가하고 기존 항목에 "→ D-XX로 대체됨"을 표기한다.
> 회의가 끝나면 **이 파일만** 갱신한다.
>
> 최종 수정: 2026-08-17

---

## 1. 확정 결정

| # | 결정 | 확정일 | 근거 |
|---|---|---|---|
| **D-01** | 환급 커브 = 볼록결합 `R(x) = α·x + (1−α)·x²` | 07-19 | 01_CURVE_DESIGN §6. 증가형·S자형·3차 이상 다항식은 명시적 기각 |
| **D-02** | 분배 = **스트리밍**(잔여 기간 균등 방출). 즉시 분배 기각 | 07-19 | 01_CURVE_DESIGN §7 명제 4. 탈락 분포 왜도와 무관한 단조증가 보상 + 이탈 기회비용의 결정론적 하한 |
| **D-03** | 성공자 분배 = 예치금 지분 비례(pro-rata). 균등 분할 기각 | 07-19 | 균등 분할은 최소 베팅이 우월전략이 되어 커밋먼트 장치가 자기파괴됨 |
| **D-04** | 상호검증(같은 방 참여자 교차 확인) 기각 | 07-16 | 게임이론상 담합이 우월전략 |
| **D-05** | **커브 입력 = 완주일수 `k = d − 1`** (탈락일 d 아님). 완주자는 원금 전액 | 08-05 | 마지막 날 무임승차 제거. 구 인덱싱은 생존 임금 수열 끝에 절벽 발생 (§3) |
| **D-06** | **α = 0.2** (`alpha_bp = 2000`) | 08-05 | 초반 가혹 · 후반 보상 기조. 마지막 하루의 가치가 첫날의 4.6배 |
| **D-07** | **성공의 정의 = grace 0 (무관용).** 1회 실패 = 즉시 탈락 | 08-05 | 구현 단순화 + 남은 기간 제약. 정당성은 D-08이 보완 |
| **D-08** | **PENDING 상태 신설.** 첫 `submit_results` 전까지 자유 참여/철회(`withdraw`) | 08-05 | 무관용의 윤리적 부담 해소 — "빠질 기회를 줬는데 안 빠졌다"가 되어 day1 탈락자 환급 0이 본인 선택이 됨. 펀드의 청약 기간 / 소비자법 청약 철회기간에 해당하는 구조 |
| **D-09** | 산출물 = 컨트랙트 + **읽기전용** 대시보드. 지갑연동·백엔드 없음 | 08-05 | 14일 제약. 트랜잭션은 전부 스크립트가 CLI로 수행 |
| **D-10** | 배포 = **Sui Testnet** | 08-05 | Devnet은 주기적 초기화 |
| **D-11** | 데이터 취득 = **노드 직접 폴링**(5초). 이벤트 인덱싱 미채택 | 08-05 | 참가자 3~5명이라 성능 무관. 백엔드가 통째로 사라짐. *(08-10: 트랜스포트가 JSON-RPC → gRPC로 바뀜. 결정 자체는 유지, 수단만 변경 — D-23)* |
| **D-12** | 역할: A 컨트랙트=진모 / B 대시보드=승준 / C 오라클·데모=유안 | 08-05 | |
| **D-13** | **8/9 인터페이스 프리즈**, 8/17 코드 프리즈, 8/18 마감 | 08-05 | B·C가 안정된 시그니처 위에서 작업하기 위함 |
| **D-14** | 데모 파라미터 `total_days = 5`, day 간격 5~8초 | 08-05 | 기존 테스트 기대값 재사용. 폴링 5초를 화면이 따라올 수 있는 속도 |
| **D-15** | **인터페이스 프리즈 선언.** 대상 = 함수 시그니처 · 이벤트 필드 · struct 필드 · 호출 순서 규약. 변경은 3인 합의 | 08-09 | 인터페이스 변경은 변경자에게 5분, 나머지 둘에게 원인 불명의 몇 시간. §8.1 |
| **D-16** | **대시보드 = 1순위 필수 산출물.** CLI 실연 경로는 제거하지 않고 백업으로 유지 | 08-09 | CLI는 오라클 스크립트의 부산물이라 추가 비용 0. 발표 당일 사고 시 전환 경로. §8.2 |
| **D-17** | 대시보드에 **지갑 서명 트랜잭션 미구현** (join / claim 버튼 없음) | 08-09 | D-09의 명시적 범위 확정. 서명 플로우는 소요 예측이 어렵고 차별점 증명에 무기여. §8.3 |
| **D-18** | 데모는 **방 생성·참여를 사전 세팅한 상태**에서 시작 | 08-09 | 무대에서 다수 서명은 실패 리스크. 부수 효과로 빈 방 `submit_results` 오인 전멸이 차단됨. §8.4 |
| **D-19** | **표시용 파생값(환급률·완주일수)은 프론트에서 계산.** 뷰 함수 추가 안 함 | 08-09 | 돈이 움직이는 계산만 온체인. 표시 편의를 위한 재배포는 크리티컬 패스에 잡음. §8.5 |
| **D-20** | 배포 ID 4종의 정본 = **`docs/DEPLOYMENT.md`** | 08-09 | 카톡·개인 메모는 사본. 불일치 시 파일이 우선. §8.6 |
| **D-21** | 참가자 `Table` 조회 로직은 **`scripts/read_state.ts`로 공용화** (담당: 진모) | 08-09 | dynamic field 2단계 조회는 컨트랙트 저장 구조에서 파생된 문제. §8.7 |
| **D-22** | **빈 방 `submit_results` 가드는 도입하지 않는다.** 운용 규칙으로 회피 | 08-10 | `Published.toml` 커밋 이후 재배포가 선행 작업을 요구하고 CLI 프로토콜 버전도 뒤처져 비용이 큼. 데모는 사전 세팅(D-18)이라 발생 경로가 없음. §9.1 |
| **D-23** | **체인 접근은 gRPC(`SuiGrpcClient`)로 한다.** JSON-RPC 미사용 | 08-10 | 공개 풀노드의 JSON-RPC가 폐지되어 `SuiClient`는 전 메서드가 `MethodNotFound`. D-11의 수단만 교체. §9.2 |
| **D-24** | **데모 자금 = 고정 주소 재사용 + `pay-sui` 분배, 코인 분할은 PTB** | 08-10 | testnet faucet CLI 폐지(Web UI 캡차 필수)로 자동화 불가. "새 주소 → faucet" 흐름 폐기. §9.3 |
| **D-25** | **`read_state.ts`는 프론트로 복사해서 쓴다.** alias·공용 패키지 분리 안 함 | 08-10 | `scripts/`가 별도 npm 패키지라 상대경로 import 불가. 남은 기간 대비 과투자. 복사본이 어긋날 위험보다 세팅 지연이 크다 |
| **D-26** | **`@mysten/sui` 버전 통일 — 결정 대기.** 현재 `scripts` 1.x / `frontend` 2.x로 갈려 있음 | — | gRPC 우회 코드가 SDK 내부 구조에 의존해서 메이저 버전이 다르면 한쪽만 조용히 깨진다. §10.1 |
| **D-27** | **실배포본 검증 완료. 재배포 없음** — 방③에서 5일 시나리오 온체인 관통, 파이썬 미러와 완전 일치, dust 0 | 08-15 | 온체인 검증은 이번이 처음이다. 종전 테스트 25개는 전부 로컬 시뮬레이션이었다. §10.2 |
| **D-28** | **α = 0.2의 근거 확립.** "직관 기반" 표기 제거 (D-06 보강) | 08-17 | 목표 배율 5~10배 → 역산 구간 [0.154, 0.310]에 0.2가 들어간다. 배율은 α에 대한 쌍곡선이라 극값이 없어 원래 역산으로만 정할 수 있는 값이다. §10.3 |

## 2. 임시 규칙 (구현되어 있으나 근거 약함 — 후속 확정 대상)

| # | 규칙 | 비고 |
|---|---|---|
| **T-01** | 전원 탈락 시 각자 원금 반환 (챌린지 무효) | 유지 |
| **T-02** | 전멸(최후 생존자 동시 탈락) 시 조기 ENDED, 미방출 스트림은 vault 잔류 | D-05로 심각도가 올랐으나 **로드맵 이관으로 종결** — 데모는 생존자 1명 이상이 보장된다 (§4) |
| **T-03** | ver4 중도참여자 커브 정규화: `D_개인 = D − start_day + 1`, `k_개인 = 탈락일 − start_day` | Design I(개방형) 기준 |
| **T-04** | 참여 마감선 τ 없음 (`current_day < total_days`이면 언제든 중도참여 가능) | §4 스나이핑 이슈 |

## 3. D-05 적용 후 커브 (α = 0.2, D = 5, stake 10 SUI)

| 입력 | 상황 | 환급 (MIST) | SUI | 하루 더 버틴 값 |
|---|---|---|---|---|
| k = 0 | day1 탈락 | 0 | 0.00 | — |
| k = 1 | day2 탈락 | 720,000,000 | 0.72 | +0.72 |
| k = 2 | day3 탈락 | 2,080,000,000 | 2.08 | +1.36 |
| k = 3 | day4 탈락 | 4,080,000,000 | 4.08 | +2.00 |
| k = 4 | day5 탈락 | 6,720,000,000 | 6.72 | +2.64 |
| k = 5 | 완주 | 10,000,000,000 | 10.00 | **+3.28** |

전 구간 MIST 정수로 정확히 떨어진다 (분자 `10000k + 8000k²`, 분모 250000).
생존 임금이 전 구간 단조증가하며, 구 인덱싱에 있던 마지막 날 절벽(`… 2.56, 3.12, 0`)이 사라진다.

**부수 성질.** α = 1(순수 선형)일 때 몰수액은 `s(D−k)/D`, 스트림 길이는 `D−d+1 = D−k`이므로

```
일일 drip 증가분 = [s(D−k)/D] / (D−k) = s/D     (탈락 시점과 무관한 상수)
```

탈락 시점에 관계없이 방출량 증가분이 정확히 `stake/D`로 일정하다. **새 인덱싱에서만 성립하며**, T1 시나리오의 dust = 0이 우연이 아니라 구조적으로 보장된다는 뜻이다.

## 4. 미해결 (후속 과제)

| 항목 | 내용 |
|---|---|
| ~~**day1 탈락자 claim abort**~~ | **종결.** 대시보드가 읽기 전용이라(D-17) claim 호출 경로 자체가 없다. `claimable = 0`은 커브 정의상(`k=0 → R(0)=0`) 정상값이며, 대시보드는 이를 "전액 몰수"로 표시한다 |
| ~~**day1 전멸**~~ | **로드맵 이관** (T-02와 함께). 데모 시나리오상 생존자가 항상 1명 이상이라 발생 경로가 없다. 실서비스화 시 재검토 |
| **ver4 스나이핑 (T-04)** | Design I(개방형)은 늦게 합류할수록 일당 차익이 커진다(최대 `0.3713 @ g=29`). Design II(빈티지)/III(NAV)는 `g=1`에서 최대라 이 구멍이 없다. MVP는 I + 마감선 τ, 원칙적 해법은 II 권고 |
| **α 정밀 캘리브레이션** | 설계 근거는 D-28로 확립됨(목표 배율 역산). 남은 것은 **실측 이탈률을 확보한 뒤 목표 배율 자체를 재조정**하는 일이며, 이는 실서비스 데이터가 있어야 가능하다 |
| **grace day 도입 여부** | D-07에서 무관용으로 확정했으나, 실전 파라미터로는 `n = ⌊0.15D⌋` 검토 가치 있음. 도입 시 "grace 쓴 날은 그날 배당 제외" / "완주자는 r=1" / "마지막 날 사용 불가" 3원칙 필요 |

## 5. 닫은 안건 (더 논의하지 않음)

- **챌린지 종류 확정** → 프로토 스코프 밖. "시간 기반 체크인" 하나로 고정
- **체크인 윈도우 정책** → 시간 검증 자체를 구현하지 않으므로 무의미
- **ver5 config 추상화** → 설계 문서로만. 구현하지 않음
- **A/B/C 커브 방향 프레임** → 폐기. "인센티브 조건 정의 → 함수족 유도"로 대체 (D-01)
- **No-Loss 모드, zkLogin, 인증 강화** → 로드맵 (발표 자료용)

## 6. 현재 상태

- **완료 (컨트랙트)** — 정산 프로토 ver1~ver4 구현, `sui move test` **25개 통과**, 메커니즘 설계 전체 확정, D-05·D-06·D-08 코드 반영, **Testnet 배포**(ID는 `DEPLOYMENT.md`), **8/9 인터페이스 프리즈 선언**(D-15), **`read_state.ts` 공용화**(D-21), **α 근거 확립**(D-28)
- **완료 (검증)** — **실배포본 5일 관통 검증**(D-27). 방③에서 A=51,840,000 / B=0 / C=8,160,000 MIST, 합계 60,000,000 = 예치 60,000,000, **dust 0**. 파이썬 미러와 완전 일치. 재배포는 없다
- **완료 (파트)** — 오라클·데모 스크립트(`seed.ts`·`submit.ts`, 유안), 읽기 전용 대시보드 구현(승준)
- **진행 중** — **3자 통합 리허설.** 방④(`0xe11d8984…`, PENDING)로 진행한다 — 방③은 검증 기준점이라 상태를 보존해야 해서 submit 금지
- **각 파트 시작점** — `scripts/QUICKSTART.md`(유안), `frontend/QUICKSTART.md`(승준). 명령어·예상 출력·에러 원인이 단계별로 붙어 있다
- **블로커** — 없음. 빈 방 가드는 **미도입으로 종결**(D-22)
- **미결** — `@mysten/sui` 메이저 버전이 `scripts` 1.x / `frontend` 2.x로 갈려 있다 (D-26, 결정 대기)
- **주의** — JSON-RPC 폐지로 `SuiClient`는 전부 실패한다. 승준·유안 모두 `SuiGrpcClient`를 써야 한다 (D-23)

## 7. 남은 마일스톤

날짜 기반 일정은 폐기했다 (인터페이스 프리즈·배포·병렬 개발이 모두 끝나 의미가 없어졌다).
남은 것은 순서가 고정된 4단계이고, 각 단계는 아래 완료 판정을 통과해야 다음으로 넘어간다.

| # | 마일스톤 | 완료 판정 |
|---|---|---|
| 1 | **3자 통합 리허설** ← 현재 | 방④에 `submit.ts`를 누르면 대시보드 화면 값이 폴링으로 따라 바뀌는 것을 3인이 동시에 확인 |
| 2 | 디자인 · 커브 시각화 | 대시보드가 참가자별 탈락일·claimable에 더해 환급 커브를 그리고, 발표 화면에 그대로 띄울 수 있는 상태 |
| 3 | 슬라이드 | S1~S7 전 슬라이드가 현행 수치(D-27 실측값·D-28 α 근거)로 채워지고, 발표자별 담당 구간이 정해짐 |
| 4 | 최종 리허설 | 발표용 방을 새로 판 뒤(D-18) 대본대로 5분 안에 끊김 없이 완주. 사고 시 CLI 백업 경로(D-16) 전환까지 1회 연습 |

## 8. 8/9 인터페이스 프리즈 회의 상세 (D-15 ~ D-21)

### 8.1 D-15. 인터페이스 프리즈 선언 및 그 범위

프리즈는 "코드 동결"이 아니라 **"남이 의존하는 부분을 말없이 바꾸지 않는다"**는 약속이다.

**프리즈 대상** (변경하려면 3인 합의 필요)

| 대상 | 이유 |
|---|---|
| public 함수 이름 · 인자 순서 · 타입 | 오라클 스크립트가 직접 호출 |
| 이벤트 필드 이름 · 타입 | 외부에서 참조 가능 |
| struct 필드 이름 · 타입 (`stake`, `failed_day`, `claimable` 등) | 대시보드가 이 이름으로 값을 읽음 |
| 함수 호출 순서 규약 | 데모 시나리오가 여기 묶임 |

**프리즈 비대상** (각자 재량)

| 대상 | 이유 |
|---|---|
| 배포 ID (Package / Challenge / Table / 오라클) | 이름이 아니라 주소. `DEPLOYMENT.md` 갱신 + 통보로 끝 |
| `total_days`, 예치금 등 파라미터 **값** | 값이 바뀌어도 인터페이스는 동일 |
| 함수 내부 구현 (assert 추가, 계산 정리 등) | 외부에서 관측되지 않음 |
| UI 디자인, 스크립트 내부 구조 | 남이 의존하지 않음 |

**변경이 필요할 때의 절차**

1. 혼자 고치고 나중에 통보하는 것 **금지**
2. 단톡에 먼저 제안 + 영향받는 파트 명시
3. 3인 확인 후 변경
4. 변경 완료 통보

근거: 3인이 비동기로 작업하는 구조에서, 인터페이스 변경은 변경자에게는 5분이지만 나머지 둘에게는 원인 불명의 몇 시간이 된다. 되돌리는 비용이 타인에게 전가되는 지점만 합의 대상으로 둔다.

### 8.2 D-16. 대시보드를 1순위 산출물로 확정

대시보드는 필수 산출물이며 최우선 순위다. 동시에 CLI 실연 경로도 제거하지 않고 유지한다.

- CLI 실연은 별도로 만드는 것이 아니라, 오라클 스크립트를 만들면 자동으로 확보되는 것이다 (추가 비용 0)
- 목적: 발표 당일 RPC 지연·지갑 문제 등 사고 시 전환 가능한 경로 확보
- 이는 스코프 축소가 아니라 **백업 경로 유지**다

**대시보드 구현 우선순위**

| 순위 | 항목 | 완료 정의 |
|---|---|---|
| 1 | RPC 연결 | Challenge 오브젝트가 콘솔에 찍힘 |
| 2 | ID config 분리 | 코드에 ID 하드코딩 0건 |
| 3 | **참가자 목록 표 렌더** | 주소/예치금/상태가 표에 표시 ← **최소 성립선** |
| 4 | 방 정보 표시 | `current_day`, vault, status 표시 |
| 5 | claimable + 탈락일 표시 | day1 탈락자가 0원으로 표시됨 |
| 6 | 새로고침 / 폴링 | submit 후 화면 값이 갱신됨 |
| 7 | 디자인 | — |

### 8.3 D-17. 대시보드는 읽기 전용 — 지갑 서명 트랜잭션 미구현

대시보드에서 join / claim 등 트랜잭션 전송 기능은 구현하지 않는다.

- D-09("읽기 전용 대시보드")의 재확인 및 명시적 범위 확정
- 상태 변경은 전부 오라클 스크립트(CLI)로 수행
- 근거: 지갑 서명 플로우는 초심자 기준 소요 시간 예측이 어렵고, 데모에서 발표 흐름을 끊는 요소이며, 프로젝트 차별점(시간가중 차등 몰수) 증명에 기여하지 않는다

### 8.4 D-18. 데모는 방 생성·참여를 사전 세팅한 상태에서 시작

발표 무대에서 `create_challenge` / `join`을 실행하지 않는다. 방 생성과 참가자 등록을 사전에 완료한 상태에서 데모를 시작한다.

- 무대에서 지갑 다수 서명은 실패 리스크가 크고 발표 시간을 잠식함
- 부수 효과: **참가자 0명 상태에서 `submit_results` 호출 시 전멸로 오인되어 즉시 ENDED되는 상황이 원천 차단됨**
- 운용 규칙: **빈 방에 `submit_results`를 호출하지 않는다.** 방 생성 후 참가자를 채운 뒤 진행 (오라클 온보딩 문서에 명시)

### 8.5 D-19. 표시용 계산은 프론트엔드, 금액 계산만 온체인

환급률(%), 완주일수 등 표시 목적의 파생값은 프론트엔드에서 계산한다. 이를 위해 컨트랙트에 뷰 함수를 추가하지 않는다.

- 커브 수식(`k = d-1`, `r(d) = α·(d/D) + (1−α)·(d/D)²`, α=0.2)은 확정 상태이므로 프론트에서 계산해도 온체인 값과 일치
- **원칙**: 돈이 실제로 움직이는 계산만 온체인, 화면에 보여주기 위한 계산은 프론트
- 근거: 표시 편의를 위한 컨트랙트 수정은 재배포를 유발하며, 이는 크리티컬 패스(대시보드)에 불필요한 잡음을 넣는다

### 8.6 D-20. 배포 정보의 정본 위치

배포 ID 4종의 정본은 `docs/DEPLOYMENT.md` 한 곳으로 한다.

- 재배포 시 절차: ① `DEPLOYMENT.md` 갱신 → ② PR → ③ 단톡 통보
- 각자 코드에서는 config 파일/환경변수 한 군데로 모아 참조 (파일명·형식은 각자 재량)
- 카톡·개인 메모의 ID는 사본이며, 불일치 시 이 파일이 우선

### 8.7 D-21. 참가자 Table 조회 로직은 공용 스크립트로 제공

`Table` dynamic field 조회 로직을 `scripts/read_state.ts`로 공용화하며, 작성 책임은 **진모(컨트랙트 담당)**가 진다.

- `Table`은 dynamic field로 저장되어 `getObject`로 내용 조회 불가 → 2단계 조회 필요
- 이는 프론트엔드 구현 난이도 문제가 아니라 **컨트랙트의 저장 구조에서 파생된 문제**이므로 컨트랙트 담당이 해소한다
- 프론트엔드(승준)와 오라클(유안) 양쪽에서 재사용
- **2026-08-10 완료.** 조회 로직은 export, CLI 실행부는 분리 — 대시보드는 `import { readState }`로 가져다 쓰고 출력만 자기 방식으로 하면 된다

---

## 9. 8/10 구현 중 확정 (D-22 ~ D-24)

> 회의가 아니라 `scripts/` 착수 과정에서 실측으로 드러난 사항들이다. 셋 다 외부 환경 변화가 원인이다.

### 9.1 D-22. 빈 방 `submit_results` 가드 — 미도입 확정

참가자 0명인 방에 `submit_results`를 호출하면 생존자 지분합이 0이라 전멸로 판정되어
`status`가 즉시 `ENDED`로 잠긴다 (`challenge.move:245`). 되돌릴 수 없다.
가드를 넣을지 8/9 회의에서 보류했고, **넣지 않는 것으로 종결**한다.

- **재배포 비용이 예상보다 크다.** `Published.toml`을 커밋한 뒤로 `sui client publish`가
  `Your package is already published. You have to manually remove the publication entry`로 거부한다
  (실측). `[published.testnet]` 항목 삭제 → publish → 새 값 재커밋 절차가 붙는다.
  `sui client upgrade`로 UpgradeCap을 쓰는 길도 있으나 어느 쪽이든 크리티컬 패스에 잡음이다
- 로컬 CLI 프로토콜 버전(129)이 testnet(132)보다 낮아 재배포 시 의존성 검증 오류 위험이 있다
- 데모는 사전 세팅으로 시작하므로(D-18) 발표 중 발생 경로가 없다
- **대체 방어**: ① 운용 규칙 "빈 방에 submit 금지" ② `read_state.ts`가 빈 방을 감지하면 경고를 출력
- ⚠️ 단, 기존 배포 방 `0xfb1eb0…`이 실제로 빈 방이다. **개발 중 리허설에서 이 ID로 submit하면 그대로 터진다.** 검증용 방(§DEPLOYMENT.md)을 쓸 것

### 9.2 D-23. 체인 접근은 gRPC로

공개 풀노드의 JSON-RPC가 폐지되었다. `@mysten/sui`의 `SuiClient`로 testnet을 치면
`getObject` / `getDynamicFields` / `getCoins` / `getBalance` 등 **시도한 7개 메서드가 전부**
`MethodNotFound: JSON-RPC on public fullnodes has been deprecated`로 실패한다 (실측).

- 대안은 `SuiGrpcClient`(gRPC-web). 실측으로 정상 동작 확인
- **D-11의 결정(직접 폴링, 인덱싱 미채택)은 그대로다.** 수단만 JSON-RPC → gRPC로 바뀐다
- 인터페이스 프리즈(D-15) 대상이 아니다 — 컨트랙트 시그니처는 불변, 클라이언트 접근 방식만 변경
- 함정 2개는 `frontend/README.md`·`scripts/README.md`·`read_state.ts` 상단에 명시했다
  (생성자 옵션은 `url`이 아니라 `baseUrl` / `core.getObject()` 대신 `ledgerService` + readMask `json`)

### 9.3 D-24. 데모 자금 조달과 코인 분할

**testnet faucet CLI가 폐지되었다.** `sui client faucet`은 Web UI 안내만 출력하고 종료한다(exit 1).
캡차가 필요해 스크립트로 자동화할 수 없다.

- "리허설마다 새 주소를 만들고 faucet으로 채운다"는 흐름은 **성립하지 않는다.** 폐기
- **고정 주소를 재사용한다.** 검증용 참가자 주소 2개(`verify-b`, `verify-c`)를 만들어 두었고
  충전은 이미 SUI를 가진 주소에서 `pay-sui`로 분배한다 (한 트랜잭션으로 다수 주소 동시 가능)
- **코인 분할은 PTB로 한다.** 회의에서 정한 "faucet 복수 수령으로 코인 오브젝트를 늘린다"는 대책은
  faucet 폐지로 무효다. 대신 PTB에서 가스 코인을 직접 쪼개면 **코인 오브젝트가 1개여도 동작한다**:

  ```bash
  sui client ptb \
    --split-coins gas "[100000000]" \
    --assign stake \
    --move-call <PKG>::challenge::join @<CHALLENGE> stake.0 \
    --gas-budget 30000000
  ```

  `sui client split-coin`이 실패하던 원인(쪼갤 코인 = 가스 코인)이 구조적으로 사라진다.
  실제로 코인 1개만 가진 주소 2개가 이 방식으로 join에 성공했다
- 유안의 `seed.ts`는 이 패턴 기준으로 재설계한다

---

## 10. 8/15~8/17 검증 중 확정 (D-26 ~ D-28)

> 실배포본을 실제로 돌려본 결과와, 발표 대비로 α 근거를 정리하면서 나온 사항들이다.

### 10.1 D-26. `@mysten/sui` 버전 통일 — 결정 대기

**현재 `scripts`는 `^1.30.0`, `frontend`는 `^2.24.0`으로 메이저 버전이 갈려 있다** (실측).

- 문제는 단순한 버전 차이가 아니라 **gRPC 우회 코드가 SDK 내부 구조에 의존한다**는 점이다.
  D-23의 함정 2개(생성자 옵션이 `baseUrl` / `core.getObject()` 대신 `ledgerService` + readMask `json`)는
  공식 API 표면이 아니라 내부 구조를 짚어 쓴 것이라, 메이저 버전이 바뀌면 조용히 어긋날 수 있다
- 위험한 조합은 **D-25**다. `read_state.ts`를 프론트로 **복사**해서 쓰기로 했으므로,
  같은 소스가 서로 다른 SDK 메이저 위에서 돌아간다. 한쪽만 깨지면
  "내 쪽에서는 되는데" 상태가 되어 원인 추적이 오래 걸린다
- **아직 통일 여부·기준 버전이 결정되지 않았다.** 회의에서 정하면 이 항목을 갱신한다.
  현시점 기록은 "갈려 있다는 사실과 그것이 왜 위험한지"까지다

### 10.2 D-27. 실배포본 검증 완료 — 재배포 없음

방③(`0x2f6f1b32…`)에서 **day1 B탈락 / day3 C탈락 / A완주** 시나리오를 5일 + finalize까지
온체인으로 관통시켰다. 실측 claimable은 다음과 같다.

| 참가자 | stake (MIST) | failed_day | claimable (MIST) |
|---|---|---|---|
| A | 20,000,000 | 0 (완주) | **51,840,000** |
| B | 20,000,000 | 1 | **0** |
| C | 20,000,000 | 3 | **8,160,000** |
| 합계 | 60,000,000 | | **60,000,000** |

**합계 60,000,000 = 예치 60,000,000, dust 0.** 파이썬 미러와 완전히 일치했다
(대조 검증기: `03_시뮬레이터/verify_onchain.py`, 전 항목 OK).

**이 검증이 갖는 의미**는 "테스트를 한 번 더 돌렸다"가 아니다.
**종전 `sui move test` 25개는 전부 로컬 시뮬레이션이었고**, 실제 배포된 바이트코드가
실제 체인 위에서 같은 값을 내는지는 확인된 적이 없었다. 이번이 처음이다.

관통으로 함께 검증된 항목:

| 항목 | 근거 결정 |
|---|---|
| 볼록결합 환급 커브 | D-01 |
| 커브 인덱싱 `k = d − 1` (day1 탈락 → 0, 완주 → 전액) | D-05 |
| 스트리밍 방출 (잔여 기간 균등) | D-02 |
| `acc_per_share` 단일 절단 규약 (정산 시 1회) | PROTO_SPEC §1-3 |
| 보존 법칙 `∑수령 + dust = ∑예치` | CLAUDE.md §5-7 |

- **재배포는 하지 않는다.** 배포본이 기대대로 동작함이 확인됐으므로 손댈 이유가 없다
- ⚠️ **방③에는 다시 submit하지 마라.** 이 표가 나온 기준점이라 상태 그대로 보존해야 한다.
  리허설은 방④(`0xe11d8984…`)로 한다

### 10.3 D-28. α = 0.2의 근거 확립 — "직관 기반" 표기 제거

D-06에서 α=0.2를 확정했으나 §4에는 "현재 0.2는 직관 기반"이라고 적혀 있었다.
발표에서 "왜 0.2냐"는 질문이 나올 자리라 근거를 정리했고, **직관이 아니었음**이 드러났다.

**(1) 애초에 최적화로 구할 수 있는 값이 아니다.** 생존 임금
`w(d) = stake·[α/D + (1−α)(2d−1)/D²]`를 전개하면 마지막날/첫날 배율이 닫힌 형태로 떨어진다.

```
w(D)/w(1) = (2D−1−(D−1)α) / (1+(D−1)α)
```

α에 대한 **쌍곡선 감소라 극값이 없다.** "수학적으로 최적인 α"는 존재하지 않는다는 뜻이고,
따라서 α는 **목표 배율 T를 먼저 정하고 역산하는 값**이다 — `α = (2D−1−T)/[(D−1)(T+1)]`.

**(2) 역산 결과 0.2는 구간 안에 있다.** D=30 기준으로 목표 배율 5~10배를 넣으면
**α ∈ [0.154, 0.310]**이 나온다. 채택값 0.2가 이 구간에 들어간다.
구간을 벗어날 수 없는 이유는 양 끝이 모두 실패 모드이기 때문이다 — α=1.0이면 배율이 1.00배로
완전히 평탄해져 시간가중 차등이라는 핵심 차별점 자체가 사라지고, α=0.0이면 초반이 사실상
전액 몰수가 되어 초반 이탈자에게 "어차피 다 잃었다"는 심리를 줘 커밋먼트 장치로 역효과가 난다.

**(3) α는 총액을 바꾸지 않는다.** `Σ_{d=1..D} w(d) = stake`(망원합)이므로 **총 환급액은 α와 무관**하고,
모든 α 곡선은 `d = (D+1)/2`에서 교차한다(그 값은 `stake/D`, α 무관).
즉 α는 파이의 크기가 아니라 **교차점을 축으로 한 회전 — 시간 배분만** 조절한다.
"α를 낮추면 참가자가 손해"라는 오해를 정확히 반박하는 지점이다.

**(4) 배율은 D에 의존한다.** 같은 α=0.2에서 **D=5는 4.6배, D=30은 7.8배**다.
데모 방(D=5)의 체감 압박은 실서비스 상정(D=30)보다 약하다 — 발표에서 배율을 말할 때
어느 D 기준인지 밝혀야 한다.

- 근거 스크립트: `03_시뮬레이터/alpha_sweep.py` (닫힌 형태 ↔ 실측 수열 대조 검산 포함)
- 그래프: `04_그래프/03_alpha_sweep_tradeoff.png`(트레이드오프), `04_staying_wage_by_alpha.png`(교차점)
- §4의 "α 정밀 캘리브레이션"은 종결이 아니라 **성격이 바뀌었다** — 남은 것은 설계 근거가 아니라
  실측 이탈률로 목표 배율 자체를 재조정하는 일이며, 실서비스 데이터가 있어야 가능하다
