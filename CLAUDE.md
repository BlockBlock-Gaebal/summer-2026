# CLAUDE.md — Locked In / 갓생 내기 (팀 공통 컨텍스트)

> 저장소 루트. Claude Code 실행 시 자동 로드된다.
>
> **이 파일에는 "잘 안 바뀌는 것"만 둔다** — 프로젝트 정체성, 아키텍처 원칙, 협업 룰.
> 확정된 설계 결정과 현재 진행 상태는 **`docs/DECISIONS.md`가 유일한 정본**이다.
> 두 문서가 충돌하면 DECISIONS.md가 이긴다. 이 파일을 매번 갱신하려 하지 마라.

---

## 1. 한 줄 요약

**Locked In** (국내 청중용 병기: **갓생 내기**) — *Locked in. Paid out.*

**의지력에 돈을 거는 탈중앙 습관 챌린지 프로토콜.** 예치 → 매일 체크인 → 실패 시 시간가중 차등 몰수 → 생존자에게 스트리밍 분배.

핵심 차별점은 하나다: **탈락 시점에 따라 몰수액이 달라지는 정산 수식을 온체인으로 신뢰 불필요하게 집행하는 것.** 기존 습관 챌린지는 성공/실패 이분법이라 day1 포기와 day29 포기가 동일하게 처리된다. 이 프로토콜은 실패에 등급을 매긴다.

## 2. 팀

연세대학교 블록체인 동아리 블록블록, 3인. 전원 개발 첫 입문(크립토 도메인 지식은 헤비유저 수준). 토큰 발행·수익화 목적 없음 — 개발 경험과 출품이 목적.

| 파트 | 담당 | 디렉터리 |
|---|---|---|
| A. 컨트랙트 · 메커니즘 설계 | 하진모 | `contracts/` |
| B. 대시보드 | 정승준 | `frontend/` |
| C. 오라클 · 데모 | 김유안 | `scripts/` |

## 3. 산출물 범위

| 만든다 | 만들지 않는다 |
|---|---|
| Move 컨트랙트 + 테스트 | 지갑 연결 (dapp-kit) |
| Sui Testnet 배포 | 예치 / claim UI 버튼 |
| **읽기 전용** 대시보드 | 백엔드 서버 |
| 오라클 · 데모 자동화 스크립트 | 체크인 인증 로직 |
| | 로그인, 반응형, 다크모드 |

**대시보드는 트랜잭션을 만들지 않는다.** RPC 폴링으로 읽어서 렌더만 한다. 모든 쓰기는 `scripts/`가 CLI로 수행한다.

## 4. 기술 스택

| 레이어 | 스택 |
|---|---|
| 체인 | **Sui Testnet** (Devnet 아님 — 주기적 초기화 리스크) |
| 컨트랙트 | Sui Move 2024 에디션, Sui CLI |
| 대시보드 | Vite + React + TypeScript, `@mysten/sui`의 `SuiGrpcClient` (읽기 전용) |
| 스크립트 | TypeScript + `@mysten/sui`의 `SuiGrpcClient` |
| 환경 | WSL(Ubuntu), Node v20, npm, VS Code |
| 저장소 | GitHub `BlockBlock-Gaebal/summer-2026` |

> **⚠️ 공개 풀노드의 JSON-RPC는 폐지되었다.** `SuiClient`를 쓰면 전 메서드가 `MethodNotFound`로 실패한다.
> 반드시 `SuiGrpcClient`를 쓸 것 (D-23). 생성자 옵션은 `url`이 아니라 `baseUrl`이며,
> `core.getObject()`는 조용히 틀린 값을 주므로 `ledgerService` + readMask `json`을 쓴다.
> 조회 로직은 `scripts/read_state.ts`에 공용화되어 있으니 직접 짜지 말 것.
>
> 패키지 매니저는 실제 환경에 yarn이 없어 npm을 쓴다 (`scripts/package-lock.json`).

## 5. 아키텍처 원칙

1. **돈은 온체인, 인증은 오프체인.** 예치·정산·claim은 Move가 집행한다. 체크인 판정은 오라클 주소가 결과만 제출한다(현재는 사람이 스크립트로 호출). 이 경계 덕분에 나중에 무엇을 붙여도(BE 자동화 / 커뮤니티 투표 / AI) 컨트랙트는 불변이다.
2. **정산 수식은 순수 함수로 격리한다.** 상태 접근 없이 입력→출력만 하는 함수로 두면 테스트가 곧 수식 검산이 된다.
3. **pull 패턴.** 일괄 분배 루프는 금지. 각자 `claim`으로 찾아간다.
4. **`Table`은 키 순회가 불가능하다.** 정산 시 순회하려면 `participant_list: vector<address>`를 병행 유지해야 하고, 둘의 정합성은 항상 함께 갱신한다.
5. **곱셈 먼저, 나눗셈은 마지막 1회.** 중간값은 u128 캐스팅이 기본. 절단 횟수를 줄이는 것이 dust를 줄이는 것이다.
6. **시간 검증 없음.** `submit_results` 호출이 곧 day 진행(수동 카운터). `Clock`은 프로토 스코프 밖이다.
7. **보존 법칙이 최종 방어선이다.** `∑수령액 + dust = ∑예치액`. 모든 시나리오 테스트는 이걸 검증해야 한다.

## 6. AI 작업 지침

- 팀원은 개발 입문자다. 코드에 개념 설명을 곁들여라. 단 DeFi 도메인(AMM / 스테이킹 / 슬래싱 / LP 지분 / acc_per_share)은 설명 없이 써도 된다.
- **언어는 Sui Move다 (Solidity 아님).** Move 고유 패턴(객체 소유권, Coin 리소스의 linear type, shared object, drop 없는 struct의 분해)은 처음 나올 때 "왜 이렇게 하는지"를 짚어라.
- Sui Move는 학습 데이터가 적어 옛 문법이 섞인다. 2024 에디션 기준으로 작성하고, 확신이 없으면 docs.sui.io 확인을 권하라.
- **작업 전 `docs/DECISIONS.md`를 먼저 읽어라.** 확정값과 미해결 항목이 거기 있다.
- **스코프 확장 제안 금지.** 3절 "만들지 않는다" 목록에 있는 건 제안하지 마라. 이 팀의 최대 리스크는 기술 부족이 아니라 스코프 폭발이다.
- 수치를 바꾸는 변경(커브·파라미터·정산 규칙)은 반드시 `docs/정산_프로토타입/03_시뮬레이터/`로 기대값을 먼저 검증한 뒤 테스트에 반영하라. 도구가 셋이고 용도가 다르다.

| 도구 | 언제 쓰나 |
|---|---|
| `mirror.py` | Move 로직의 파이썬 미러(연산 순서·절단 시점까지 1:1). 직접 돌리는 게 아니라 아래 둘이 정답지로 삼는 기준 구현이다. **수식을 바꾸면 여기부터 고친다** |
| `run.py` | 기대값 **전체** 회귀 검증. 커브·파라미터·정산 규칙을 건드렸으면 여기부터 돌린다. 테스트 기대값은 이 출력에서 가져온다 |
| `verify_onchain.py` | 배포된 방의 **온체인 실측값**을 미러와 대조. 새로 배포·관통했을 때만 쓴다. 방이 바뀌면 상단 시나리오 블록만 고친다 |

- α(커브 곡률)를 건드릴 때는 `alpha_sweep.py`를 함께 돌려라. α는 최적화 값이 아니라 목표 배율에서 역산하는 값이므로(D-28), 새 값이 목표 배율 구간 안인지부터 확인해야 한다.

## 7. 팀 운영 룰

1. **회의 후 결정은 `docs/DECISIONS.md`에만 기록한다** (갱신 담당: 진모). 다른 문서에 흩뿌리지 않는다.
2. **막히면 카톡.** 8시간 내 답장. 혼자 30분 이상 붙잡지 말 것.
3. **PR 리뷰**: 컨트랙트는 리뷰 필수, 대시보드·스크립트는 셀프 머지 허용.
4. **AI 코드 이해 룰**: 코드 라인 단위 이해는 불필요하다. 단 **로직(무슨 값이 어떻게 흘러 뭐가 나오는지)은 반드시 말로 설명할 수 있어야 한다.**
5. 브랜치: `contract/*`, `dashboard/*`, `demo/*` → PR → `main`. main은 항상 테스트 통과 상태를 유지한다.

## 8. 저장소 구조

```
CLAUDE.md                 이 파일 (원칙)
README.md                 외부용 소개
contracts/godsaeng/       Move 패키지 — 정산 로직 + 테스트
frontend/                 읽기 전용 대시보드
scripts/                  오라클 · 데모 자동화
docs/
├─ DECISIONS.md           ★ 확정 결정 + 현재 상태 (정본)
├─ DEPLOYMENT.md          ★ 배포 ID · 방 목록 (정본)
├─ 데모_실행절차.md        데모 시나리오 실행 · 검증 절차
├─ PROJECT_CONTEXT.md     배경 · 리서치 · 경쟁 분석 (아카이브)
└─ 정산_프로토타입/
   ├─ 01_설계문서/         커브 유도, 명제와 증명, ver4 설계 비교
   ├─ 02_스펙/             PROTO_SPEC — 구현 스펙 (아카이브)
   ├─ 03_시뮬레이터/        mirror.py(Move 로직 미러) · run.py(기대값 전체 검증)
   │                      alpha_sweep.py(α 근거·배율 역산) · verify_onchain.py(온체인 실측 대조)
   ├─ 04_그래프/           시뮬레이터 산출 그래프
   └─ 05_구현로그/          구현 당시 기록 (이력 — 갱신하지 않음)
members/                  팀원 소개
```

> 패키지 식별자는 `godsaeng`으로 남아 있다. 배포본과 묶여 있어 변경하지 않는다.
> (Move 패키지명 `godsaeng`, 모듈 `challenge`, npm 패키지 `godsaeng-scripts`, `godsaeng_sim*.py` —
> D-15 인터페이스 프리즈 · D-27 재배포 없음. 바꾸면 PACKAGE_ID가 무효가 되어 등록된 방이 전부 죽는다.)

## 9. 문서 갱신 정책

- **`DECISIONS.md`** — 회의 후 즉시 갱신. 결정은 지우지 않고 누적한다.
- **`DEPLOYMENT.md`** — 재배포·방 생성 시 갱신. 절차는 ① 파일 갱신 → ② PR → ③ 단톡 통보 (D-20).
- **설계문서 (`01_설계문서/`)** — 결정이 수치를 바꾸면 갱신한다. 이론 본문은 유지.
- **이 파일** — 원칙·역할·스택이 바뀔 때만. 진행 상황을 여기 쓰지 마라.

### 아카이브 배너 규칙

**특정 시점의 기록인 문서는 본문을 고치지 않는다.** 고치면 "그때 무엇을 알고 있었는지"가 사라져 이력이 왜곡된다. 현행과 어긋나는 부분은 **상단 배너로만** 안내한다.

대상: `05_구현로그/`, `02_스펙/PROTO_SPEC.md`, `PROJECT_CONTEXT.md`, `회의/`의 회의록.

배너 형식 (내용은 문서별로 조정):

```markdown
> 🗂 **아카이브 — YYYY-MM-DD 시점의 문서.**
> [무엇이 바뀌었는지 한 줄]
> **현재 확정 사실은 `docs/DECISIONS.md`가 정본이다.** 이 문서는 갱신하지 않는다.
```

파일을 옮기거나 지우지 않는다. 배너만 얹는다.
